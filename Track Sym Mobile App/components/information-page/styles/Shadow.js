import { StyleSheet } from 'react-native'

const Shadow = StyleSheet.create({
  shadow: {
    shadowColor: "#000",
    shadowRadius: 5,
    shadowOffset: {
      width: 0,
      height: 4,
    },
    shadowOpacity: 0.30,
    shadowRadius: 4.65,
    elevation: 3
  }
});

export default Shadow;